# ts2v — TypeScript to SystemVerilog Compiler

**IEEE 1800-2017 Compliant** | **370 Tests** | **Dual-Core CPU Demo** | **Multi-Vendor Board Support**

```
TypeScript (@Module classes) ──→ ts2v ──→ Synthesizable SystemVerilog
                                            ├─ always_ff @(posedge clk or negedge rst_n)
                                            ├─ always_comb
                                            ├─ typedef enum
                                            └─ case/endcase
```
