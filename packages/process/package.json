{
  "name": "@ts2v/process",
  "version": "2.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "bunx biome check src",
    "test": "bun test --pass-with-no-tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ts2v/types": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14"
  }
}
